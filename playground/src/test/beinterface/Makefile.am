INCLUDES = $(all_includes) -I$(srcdir)/../../interface

bin_PROGRAMS = testbeif
testbeif_SOURCES = main.cpp
testbeif_LDFLAGS = $(KDE_RPATH) $(all_libraries)
testbeif_LDADD = $(LIB_KFILE)
KDE_CXXFLAGS = -g

METASOURCES = AUTO
